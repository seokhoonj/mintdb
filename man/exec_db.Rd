% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{exec_db}
\alias{exec_db}
\title{Execute a Non-SELECT Statement}
\usage{
exec_db(sql, params = list())
}
\arguments{
\item{sql}{Character SQL statement (non-SELECT).}

\item{params}{Optional list of parameter values to bind to \verb{?} placeholders
in \code{sql}. Values are bound in order. If empty, binding is skipped.}
}
\value{
Invisibly returns an integer with the number of rows affected (when available).
}
\description{
Executes a SQL statement that does not return a result set (e.g. \code{INSERT},
\code{UPDATE}, \code{DELETE}, or DDL). Supports positional parameter binding.
}
\examples{
\dontrun{
set_db_conn_info(driver = "postgres", host = "localhost", dbname = "app",
                 user = "pg", password = "pw")
set_db_conn()

# Insert with params
exec_db("INSERT INTO logs(message, level) VALUES (?, ?)", params = list("ok", "INFO"))

# Update without params
exec_db("UPDATE users SET last_login = NOW() WHERE id = 42")

set_db_disconn()
}
}
\seealso{
\code{\link[=query_db]{query_db()}}, \code{\link[=set_db_conn]{set_db_conn()}}, \code{\link[=get_db_conn]{get_db_conn()}}, \code{\link[=has_db_conn]{has_db_conn()}}
}
