% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mintdb-package.R
\docType{package}
\name{mintdb-package}
\alias{mintdb}
\alias{mintdb-package}
\title{mintdb: Minimal Integration Toolkit for Databases}
\description{
\strong{mintdb} (Minimal Integration Toolkit for Databases) provides
a unified and lightweight interface for connecting to multiple
database backends such as MariaDB, MySQL, PostgreSQL, SQLite, and ODBC.

It wraps the standard \link{DBI} interface and optionally supports
connection pooling through the \link{pool} package.
Environment-based helpers allow you to define and reuse connection
settings easily across scripts, Shiny apps, and background services.
}
\details{
Key exported functions:
\itemize{
\item \code{\link[=set_db_conn_info]{set_db_conn_info()}} — Define and store DB credentials and connection info.
\item \code{\link[=set_db_conn]{set_db_conn()}} — Establish a connection (optionally using a pool).
\item \code{\link[=set_db_disconn]{set_db_disconn()}} — Safely disconnect the active connection or pool.
\item \code{\link[=has_db_conn]{has_db_conn()}} — Check whether an active valid connection exists.
\item Driver-specific helpers:
\code{\link[=set_mariadb_conn]{set_mariadb_conn()}}, \code{\link[=set_mysql_conn]{set_mysql_conn()}}, \code{\link[=set_postgres_conn]{set_postgres_conn()}},
\code{\link[=set_sqlite_conn]{set_sqlite_conn()}}, \code{\link[=set_odbc_conn]{set_odbc_conn()}}.
}

When the package is loaded, a private environment \code{.MINTDB_ENV} is created
to manage active connections. Upon unloading, all connections or pools are
automatically closed to ensure safe cleanup.

This design makes \strong{mintdb} suitable for persistent R sessions,
automated batch jobs, and reactive web apps where safe connection
handling is crucial.
}
\section{Persistent setup via \code{.Renviron}}{

While \code{set_db_conn_info()} is convenient for one-off sessions, you can persist
your settings using environment variables in \verb{~/.Renviron} (or a project-local \code{.Renviron}).
\enumerate{
\item Open your \code{.Renviron}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{usethis::edit_r_environ()
}\if{html}{\out{</div>}}
\item Add the variables (example: PostgreSQL):

\if{html}{\out{<div class="sourceCode ini">}}\preformatted{MINTDB_DRIVER=postgres
MINTDB_HOST=localhost
MINTDB_DBNAME=app
MINTDB_USER=postgres
MINTDB_PASSWORD=secret
MINTDB_PORT=5432
}\if{html}{\out{</div>}}

For SQLite:

\if{html}{\out{<div class="sourceCode ini">}}\preformatted{MINTDB_DRIVER=sqlite
MINTDB_FILEPATH=data/app.sqlite
}\if{html}{\out{</div>}}
\item Restart R, then simply call:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{.rs.restartR()
set_db_conn()
}\if{html}{\out{</div>}}
}

\emph{Security tip:} do \strong{not} commit \code{.Renviron} to version control. Prefer environment
variables for secrets. For advanced, profile-based setups, consider using a YAML
file and a loader (e.g., \code{load_db_conn_info_yaml("mintdb.yml", profile = "prod")}).
}

\seealso{
\code{\link[DBI:dbConnect]{DBI::dbConnect()}}, \code{\link[pool:dbPool]{pool::dbPool()}}, \code{\link[askpass:askpass]{askpass::askpass()}}, \code{\link[usethis:edit]{usethis::edit_r_environ()}}
}
\author{
\strong{Maintainer}: Seokhoon Joo \email{seokhoonj@gmail.com}

}
\keyword{internal}
