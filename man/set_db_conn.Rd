% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conn.R
\name{set_db_conn}
\alias{set_db_conn}
\alias{set_mariadb_conn}
\alias{set_mysql_conn}
\alias{set_postgres_conn}
\alias{set_sqlite_conn}
\alias{set_odbc_conn}
\title{Establish Database Connection (and Driver-Specific Wrappers)}
\usage{
set_db_conn(use_pool = TRUE, ...)

set_mariadb_conn(use_pool = TRUE, ...)

set_mysql_conn(use_pool = TRUE, ...)

set_postgres_conn(use_pool = TRUE, ...)

set_sqlite_conn(use_pool = FALSE, ...)

set_odbc_conn(use_pool = TRUE, ...)
}
\arguments{
\item{use_pool}{Logical; use a connection pool if available. Default \code{TRUE}
(except for SQLite wrappers where pooling typically provides no benefit).}

\item{...}{Additional arguments forwarded to \code{\link[DBI:dbConnect]{DBI::dbConnect()}} or
\code{\link[pool:dbPool]{pool::dbPool()}}, depending on \code{use_pool}.}
}
\value{
A DBI connection or a \code{Pool} object (also saved in \code{.MINTDB_ENV}).
}
\description{
Create a database connection (or a connection pool when available) using
parameters previously set by \code{\link[=set_db_conn_info]{set_db_conn_info()}}. This page documents
the core connector \code{set_db_conn()} and its driver-specific wrapper helpers:
}
\details{
\itemize{
\item \code{set_mariadb_conn()} — sets driver to \code{"mariadb"}
\item \code{set_mysql_conn()} — sets driver to \code{"mysql"} (uses RMariaDB)
\item \code{set_postgres_conn()} — sets driver to \code{"postgres"}
\item \code{set_sqlite_conn()} — sets driver to \code{"sqlite"}
\item \code{set_odbc_conn()} — sets driver to \code{"odbc"}
}

If \code{use_pool = TRUE} and the \pkg{pool} package is installed, a pool
is created via \code{\link[pool:dbPool]{pool::dbPool()}}. Otherwise, a plain DBI connection is
created via \code{\link[DBI:dbConnect]{DBI::dbConnect()}}. The active connection (or pool) is stored
in the internal environment \code{.MINTDB_ENV} as \code{.MINTDB_CONN}.
}
